---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/app/layout.tsx, src/components/layout/nav.tsx, src/components/layout/footer.tsx, src/lib/metadata.ts]
autonomous: true
requirements: [DSGN-03, DSGN-05]
must_haves:
  truths:
    - "Navigation uses conversational labels ('who's this guy?', etc.)"
    - "Site is responsive: navigation adapts to mobile (hamburger or bottom nav)"
    - "Layout includes a consistent Header, Main content area, and Footer"
  artifacts:
    - path: "src/components/layout/nav.tsx"
      provides: "Responsive conversational navigation component"
    - path: "src/app/layout.tsx"
      provides: "Global shell with fonts and metadata"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/nav.tsx"
      via: "rendering in root layout"
---

<objective>
Build the global responsive shell, including the root layout, metadata, conversational navigation, and footer.

Purpose: Establishes the site's structure and makes the foundation navigable.
Output: A consistent global shell that wraps all site pages.
</objective>

<execution_context>
@/Users/kadenpriebe/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/kadenpriebe/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root Layout, Fonts & Metadata</name>
  <files>src/app/layout.tsx, src/lib/metadata.ts</files>
  <action>
    Configure global fonts in `src/app/layout.tsx` using `next/font/google`. 
    Use 'Plus Jakarta Sans' for headings and 'Quicksand' (or a clean sans-serif) for body text to maintain the "warm" vibe.
    Create a `src/lib/metadata.ts` file to manage global SEO settings (Title, Description, Favicon config).
    Update `RootLayout` to apply these fonts and metadata.
  </action>
  <verify>Check page source for correct title and font-family declarations.</verify>
  <done>Global layout has proper fonts and SEO metadata configured.</done>
</task>

<task type="auto">
  <name>Task 2: Conversational Navigation Component</name>
  <files>src/components/layout/nav.tsx</files>
  <action>
    Create a `Nav` component that uses conversational naming for links:
    - About -> "who's this guy?"
    - Projects -> "what i've built"
    - Blog -> "random thoughts"
    - Links -> "let's talk"
    Ensure the nav is responsive:
    - Desktop: Horizontal links in the header or a sticky sidebar.
    - Mobile: A hamburger menu or a simplified mobile-first navigation.
    Use Tailwind's `brand-peach` or `brand-cyan` for hover/active states.
  </action>
  <verify>Resize browser to verify mobile menu appears and labels match conversational naming.</verify>
  <done>Responsive conversational navigation is functional.</done>
</task>

<task type="auto">
  <name>Task 3: Global Footer & Main Content Area</name>
  <files>src/components/layout/footer.tsx, src/app/layout.tsx</files>
  <action>
    Create a simple `Footer` component with copyright, social links (placeholders), and a small "built by Kaden" tag.
    Integrate `Nav` and `Footer` into the `RootLayout`, wrapping `{children}` in a `<main>` tag with appropriate padding/spacing.
  </action>
  <verify>All pages (currently just Home) show the Header and Footer.</verify>
  <done>The site shell is complete with Header, Main content, and Footer.</done>
</task>

</tasks>

<verification>
- Verify navigation labels are conversational.
- Check mobile responsiveness (no overflow, usable menu).
- Verify fonts are loading correctly.
</verification>

<success_criteria>
- Navigation links route to (currently non-existent) paths without crashing the app.
- Site looks professional and follows the warm palette.
- Semantic HTML tags (header, main, footer, nav) are used.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
