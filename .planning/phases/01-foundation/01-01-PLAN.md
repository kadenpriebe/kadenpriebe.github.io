---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx]
autonomous: true
requirements: [DSGN-01, DSGN-02]
must_haves:
  truths:
    - "Next.js 15 dev server starts without errors"
    - "Tailwind v4 styles are applied to the home page"
    - "The minimalist light palette (white background, black text) is available as CSS variables/Tailwind classes"
  artifacts:
    - path: "package.json"
      provides: "Next.js 15 and Tailwind v4 dependencies"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 theme configuration with minimalist light palette"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import"
---

<objective>
Initialize the Next.js 15 project with TypeScript and Tailwind CSS v4, establishing the core design system tokens (minimalist light theme).

Purpose: Provides the technical foundation and visual identity for all subsequent work.
Output: A functional Next.js 15 app with Tailwind v4 configured and theme variables defined.
</objective>

<execution_context>
@/Users/kadenpriebe/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/kadenpriebe/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 15 Project</name>
  <files>package.json, tsconfig.json, src/app/layout.tsx, src/app/page.tsx</files>
  <action>
    Initialize a new Next.js 15 project using the App Router and TypeScript. 
    Use `npx create-next-app@latest .` with the following flags: `--ts --tailwind --eslint --app --src-dir --import-alias "@/*" --no-git`.
    Ensure `package.json` reflects Next.js 15.x and React 19.x.
    Clean up default boiler plate in `src/app/page.tsx` to show a simple "Foundation" heading.
  </action>
  <verify>Run `npm run dev` and check http://localhost:3000 for "Foundation".</verify>
  <done>Next.js 15 project is initialized and running locally.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS v4 & Minimalist Light Palette</name>
  <files>src/app/globals.css</files>
  <action>
    Upgrade or ensure Tailwind CSS v4 is configured correctly.
    In `src/app/globals.css`, use the `@theme` directive to define the minimalist light palette:
    - `--color-brand-bg`: Pure white (e.g., #ffffff)
    - `--color-brand-text`: A deep, near-black (e.g., #1a1a1a)
    - `--color-brand-muted`: A soft gray for secondary text (e.g., #666666)
    Add these to the Tailwind theme so they are available as `bg-brand-bg`, `text-brand-text`, etc.
    Set the default background of the body to `brand-bg` and default text to `brand-text`.
  </action>
  <verify>Inspect the home page and verify the background color is white and text is dark.</verify>
  <done>Tailwind v4 is configured with the custom minimalist light palette tokens.</done>
</task>

<task type="auto">
  <name>Task 3: Create Core Layout Components</name>
  <files>src/components/ui/container.tsx, src/components/ui/section.tsx</files>
  <action>
    Create basic layout primitives to enforce consistency:
    - `Container`: A wrapper with max-width and horizontal padding, centered.
    - `Section`: A vertical spacing component with padding-top and padding-bottom.
    Use these to structure the basic "Foundation" content on the home page to test responsive constraints.
  </action>
  <verify>Home page uses Container and Section components, and content is centered with max-width on desktop.</verify>
  <done>Core layout primitives are available for building the site shell.</done>
</task>

</tasks>

<verification>
- Check `package.json` for correct versions.
- Verify `globals.css` contains `@theme` with custom colors.
- Verify Home page renders with the white background and centered container.
</verification>

<success_criteria>
- Dev server starts in < 5s.
- Home page is responsive and reflects the brand palette.
- No TypeScript or ESLint errors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
