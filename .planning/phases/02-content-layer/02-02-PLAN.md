---
phase: 02-content-layer
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified: [src/lib/projects.ts, src/app/projects/page.tsx, src/app/projects/[slug]/page.tsx]
autonomous: true
requirements: [PROJ-01, PROJ-02]
must_haves:
  truths:
    - "Projects are managed as local MDX files in content/projects"
    - "Projects index displays a grid/list of projects with thumbnails/metadata"
    - "Project detail pages render MDX content and show project-specific metadata (links, tech stack)"
  artifacts:
    - path: "src/lib/projects.ts"
      provides: "Project parsing and fetching logic"
    - path: "src/app/projects/page.tsx"
      provides: "Projects directory listing"
    - path: "src/app/projects/[slug]/page.tsx"
      provides: "Dynamic project rendering"
---

<objective>
Implement the projects infrastructure following the pattern established for the blog, allowing for rich project showcases with MDX and metadata.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Set up Projects Content Directory</name>
  <action>
    Create the `content/projects` directory and add a sample project MDX file (`personal-website.mdx`).
  </action>
</task>

<task type="auto">
  <name>Task 2: Create Projects Utility</name>
  <action>
    Create `src/lib/projects.ts` by adapting the logic from `src/lib/blog.ts` to handle project-specific frontmatter (e.g., `github`, `demo`, `stack`, `thumbnail`).
  </action>
</task>

<task type="auto">
  <name>Task 3: Implement Projects Index Page</name>
  <action>
    Update `src/app/projects/page.tsx` to fetch all projects and display them. Use a grid layout appropriate for project cards.
  </action>
</task>

<task type="auto">
  <name>Task 4: Implement Project Detail Page</name>
  <action>
    Create `src/app/projects/[slug]/page.tsx` to render project MDX content. Include a header section for project links (GitHub/Demo) and tech stack tags.
  </action>
</task>

<task type="auto">
  <name>Task 5: Verification</name>
  <action>
    Verify that the projects list and detail pages render correctly and that metadata is properly displayed.
  </action>
</task>

</tasks>

<verification>
- Projects index shows the "Personal Website" project card.
- Project detail page renders MDX and displays GitHub/Demo links if present.
- Build passes with static generation for all projects.
</verification>
